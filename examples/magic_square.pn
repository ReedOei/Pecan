Let a,b,c,d,e,f,g,h,i,sum be nat.

Example using natFormat of {
    @postprocess[
        a + b + c = sum &
        d + e + f = sum &
        g + h + i = sum &
        a + d + g = sum &
        b + e + h = sum &
        c + f + i = sum &

        a + e + i = sum &
        c + e + g = sum
    ] &
    distinct { a,b,c,d,e,f,g,h,i }
}.

